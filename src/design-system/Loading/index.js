import React from 'react';
import MaxPanel from '../MaxPanel';
import Text from '../Text';

const Loading = () => (
  <MaxPanel centered>
    <Text fontSize={96}>loading...</Text>
  </MaxPanel>
);

export default Loading;
